info:
  name: Admin List Categories
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/admin/categories"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists categories with admin sort order and metadata.
