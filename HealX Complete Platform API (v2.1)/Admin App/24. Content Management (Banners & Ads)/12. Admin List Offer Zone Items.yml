info:
  name: Admin List Offer Zone Items
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/admin/offer-zone"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: "Admin endpoint to list all offer zone marketing items."
