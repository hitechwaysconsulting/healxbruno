info:
  name: Admin List Staff / Admins
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/admin/admins"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists all administrator accounts and roles.
