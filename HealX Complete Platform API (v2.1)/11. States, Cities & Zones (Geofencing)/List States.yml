info:
  name: List States
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/states"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists all serviceable states.
