info:
  name: List Cities
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/cities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists all master cities.
