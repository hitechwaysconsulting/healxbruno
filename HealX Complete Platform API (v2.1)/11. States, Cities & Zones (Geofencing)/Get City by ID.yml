info:
  name: Get City by ID
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/cities/{{cityId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Retrieves details of a city.
