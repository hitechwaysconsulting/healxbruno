info:
  name: List All Zones
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/zones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists all service zones with capacity caps and booking status.
