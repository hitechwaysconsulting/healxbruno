info:
  name: Toggle State Status (Admin)
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/states/{{stateId}}/status"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "active"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Toggles state active/inactive.
