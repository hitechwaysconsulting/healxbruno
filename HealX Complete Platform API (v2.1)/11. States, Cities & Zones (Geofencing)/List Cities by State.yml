info:
  name: List Cities by State
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/states/{{stateId}}/cities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists cities belonging to a specific state.
