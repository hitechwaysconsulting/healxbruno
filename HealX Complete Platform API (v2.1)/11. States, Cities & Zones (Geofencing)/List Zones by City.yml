info:
  name: List Zones by City
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/states/{{stateId}}/cities/{{cityId}}/zones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists all operational geofenced zones within a city.
