info:
  name: Search Services
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/services/search?q=nursing&category=0"
  params:
    - name: q
      value: nursing
      type: query
      description: Search query string
    - name: category
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Full-text / trigram search across service name and description.
