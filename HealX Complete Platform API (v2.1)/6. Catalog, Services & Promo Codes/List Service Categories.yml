info:
  name: List Service Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: "Lists system categories: 0 (Medicated) and 1 (Non-Medicated) with descriptions and sort order."
