info:
  name: Get Service by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/services/{{serviceId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Retrieves complete details of a specific service including whether medical prescription is mandatory.
