info:
  name: Services Grouped by Category
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/services/grouped-by-category"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Returns catalog organized by category sections for home screen rendering.
