info:
  name: Admin Delete Promo Code
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/admin/promo-codes/{{promoCodeId}}"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Deactivates and removes promo code.
