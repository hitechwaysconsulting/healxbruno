info:
  name: Admin Toggle Service Status
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/admin/services/{{serviceId}}/status"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "active"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Activates or deactivates service in customer catalog.
