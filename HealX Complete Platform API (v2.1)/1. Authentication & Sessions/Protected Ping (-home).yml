info:
  name: Protected Ping (/home)
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/home"
  headers:
    - name: Authorization
      value: Bearer {{customerToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Sample protected route returning the current actor ID, role, and server time.
