info:
  name: Admin List Advertisements
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/admin/advertisements"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: "Admin endpoint to list all advertisements."
