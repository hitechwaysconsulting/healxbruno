info:
  name: List Public Advertisements
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/advertisements"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: "Fetches active sponsor and partner advertisements."
