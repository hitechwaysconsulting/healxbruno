info:
  name: List Public Offer Zone
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/offer-zone"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: "Fetches active offer zone promotions and discount cards."
