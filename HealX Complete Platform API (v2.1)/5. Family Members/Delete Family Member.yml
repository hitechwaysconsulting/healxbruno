info:
  name: Delete Family Member
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/user/family-members/{{familyMemberId}}"
  headers:
    - name: Authorization
      value: Bearer {{customerToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Removes the family member record.
