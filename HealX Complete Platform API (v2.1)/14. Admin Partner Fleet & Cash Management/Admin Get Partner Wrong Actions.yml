info:
  name: Admin Get Partner Wrong Actions
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/admin/partners/{{partnerId}}/wrong-actions"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Lists disciplinary incidents, customer complaints, or clinical procedure violations recorded against the partner.
